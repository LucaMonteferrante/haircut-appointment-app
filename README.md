This is how you use it:

clone the repo:

git clone https://github.com/LucaMonteferrante/haircut-appointment-app.git


if not already in project root then:

cd haircut-appointment-app



to start the app run:

docker compose up --build -d

at 

http://127.0.0.1:5000/



to stop the app do
docker compose down
